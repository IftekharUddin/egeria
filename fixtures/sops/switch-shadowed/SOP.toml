[sop]
name = "switch-shadowed"
description = "Route a deploy by rollout stage, with two unreachable switch ports"
version = "0.3.0"
priority = "critical"
execution_mode = "step_by_step"
max_concurrent = 1
admission_policy = "hold"
max_pending_approvals = 4

[[triggers]]
type = "cron"
expression = "0 */5 * * *"

[[triggers]]
type = "manual"
